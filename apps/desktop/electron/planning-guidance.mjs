// Style only. Never replace native policy, tools, user requirements or Agent Home.
export const profiles = [
 'For work requiring a plan, describe the necessary steps, dependencies, verification and failure handling. Preserve every user requirement. For simple tasks proceed directly without a separate plan.',
 'When a plan is needed, use concise actionable steps. Combine independent related work; preserve requirements, dependencies, checks and failure handling. Skip planning for simple tasks.',
 'Plan only when needed: minimal sufficient steps, explicit checks and dependencies. Keep every requirement and failure path. Execute simple tasks directly.'
];
