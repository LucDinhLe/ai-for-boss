# 0050 — Repository consolidation

Owner requests merging preview into ai-for-boss, publishing the main repository after privacy review, and deleting preview. Preserve both Git histories and release assets before deletion. Audit all reachable source history and existing evidence. Update future builds to the main release endpoint; retain signature and repository allowlists. Existing beta36 and older installers keep their embedded legacy endpoint and require a future manual upgrade once preview is removed. Do not mislabel migration as a new application build.
