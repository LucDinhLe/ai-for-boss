# Supervisor

Supervisor tương lai sẽ sở hữu vòng đời OpenClaw nhúng, cổng động, health, restart có giới hạn, Safe Mode, backup, update và rollback.

Không dùng `--force` và không giết process lạ để giải quyết xung đột cổng.
