# OpenClaw Adapter

Adapter tương lai sẽ cô lập giao diện khỏi Gateway protocol, quản lý handshake, scope, schema validation, reconnect, subscription, history, in-flight run và approval backlog.

Không đọc hoặc sửa state nội bộ của OpenClaw.
