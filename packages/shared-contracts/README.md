# Shared contracts

Vùng tương lai cho schema sản phẩm, error codes, capability manifest types và IPC contracts dùng chung.

Không sao chép Gateway types thủ công. Package chính thức của release train là nguồn upstream.
